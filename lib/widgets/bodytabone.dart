import 'package:flutter/material.dart';

class BodyTabOne extends StatelessWidget {
  const BodyTabOne({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text("EEEEEE");
  }
}